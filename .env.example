# Gavryn Local - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SERVICE PORTS (optional - defaults shown)
# =============================================================================
# CONTROL_PLANE_PORT=8080
# TOOL_RUNNER_PORT=8081
# BROWSER_WORKER_PORT=8082
# TEMPORAL_UI_PORT=8088

# =============================================================================
# DATABASE (optional - uses Docker Postgres by default)
# =============================================================================
# POSTGRES_USER=gavryn
# POSTGRES_PASSWORD=gavryn
# POSTGRES_DB=gavryn
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_URL=postgres://gavryn:gavryn@localhost:5432/gavryn?sslmode=disable

# =============================================================================
# LLM CONFIGURATION (required for AI features)
# =============================================================================

# LLM Mode: 'remote' (default) or 'local' (coming soon)
# LLM_MODE=remote

# Provider: 'codex', 'openai', 'openrouter', 'opencode-zen', 'kimi-for-coding', 'moonshot-ai'
# LLM_PROVIDER=codex

# Model name (provider-specific)
# LLM_MODEL=gpt-5.2-codex

# Base URL for API (optional - uses provider defaults)
# LLM_BASE_URL=

# Optional fallback model route (used automatically when primary provider is unavailable)
# LLM_FALLBACK_PROVIDER=openrouter
# LLM_FALLBACK_MODEL=anthropic/claude-3.5-sonnet
# LLM_FALLBACK_BASE_URL=

# API Keys (optional; the setup wizard can store these locally)
# OPENAI_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-...
# OPENCODE_API_KEY=sk-...

# Codex Configuration (if using Codex provider)
# CODEX_AUTH_PATH=/Users/YOUR_USERNAME/.codex/auth.json
# CODEX_HOME=/Users/YOUR_USERNAME/.codex

# Encryption key for storing API keys (generate with: openssl rand -base64 32)
# LLM_SECRETS_KEY=your-base64-encoded-32-byte-key

# =============================================================================
# WORKER CONFIGURATION (optional)
# =============================================================================

# Comma-separated list of allowed tools
# ALLOWED_TOOLS=browser.navigate,browser.snapshot,browser.click,browser.type,browser.scroll,browser.extract,browser.evaluate,browser.pdf,document.create_pptx,document.create_docx,document.create_pdf,document.create_csv,editor.list,editor.read,editor.write,editor.delete,editor.stat,process.exec,process.start,process.status,process.logs,process.stop,process.list

# Comma-separated commands allowed by process.exec
# PROCESS_ALLOWLIST=echo,ls,pwd,whoami,cat,cp,mv,rm,mkdir,touch,find,grep,sed,awk,git,node,npm,npx,pnpm,yarn,bun,go,python,python3,pip,pip3,uv,sh,bash

# Browser worker settings
# BROWSER_HEADLESS=true
# BROWSER_USER_TAB_CDP_URL=http://127.0.0.1:9222
# BROWSER_CDP_URL=http://127.0.0.1:9222

# Optional notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# =============================================================================
# TEMPORAL (optional - uses Docker by default)
# =============================================================================
# TEMPORAL_ADDRESS=localhost:7233

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# --- OpenAI Configuration ---
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o
# OPENAI_API_KEY=sk-your-key-here
# LLM_SECRETS_KEY=your-encryption-key

# --- OpenRouter Configuration ---
# LLM_PROVIDER=openrouter
# LLM_MODEL=anthropic/claude-3.5-sonnet
# OPENROUTER_API_KEY=sk-or-your-key-here
# LLM_SECRETS_KEY=your-encryption-key

# --- Codex Configuration ---
# LLM_PROVIDER=codex
# LLM_MODEL=gpt-5.2-codex
# CODEX_AUTH_PATH=/Users/YOUR_USERNAME/.codex/auth.json
# LLM_SECRETS_KEY=your-encryption-key

# --- OpenCode Zen Configuration ---
# LLM_PROVIDER=opencode-zen
# LLM_MODEL=opencode/gpt-5.2-codex
# OPENCODE_API_KEY=sk-your-key-here
# LLM_SECRETS_KEY=your-encryption-key

# --- Kimi for Coding Configuration ---
# LLM_PROVIDER=kimi-for-coding
# LLM_MODEL=kimi-k2-2024-12-11
# LLM_SECRETS_KEY=your-encryption-key

# --- Moonshot AI Configuration ---
# LLM_PROVIDER=moonshot-ai
# LLM_MODEL=moonshot-v1-8k
# LLM_SECRETS_KEY=your-encryption-key
